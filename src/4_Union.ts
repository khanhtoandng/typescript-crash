// Enum
enum ResourceType {
  BOOK, // index 0
  AUTHOR, // index 1
  FILM,
  PERSON,
}

// Enum2
enum Direction {
  Up = "Up",
  Down = "Down",
  Left = "Left",
  Right = "Right",
}

// Using
console.log(ResourceType.FILM); // 2
console.log(Direction.Left); // Left
